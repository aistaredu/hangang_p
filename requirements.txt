# ───────────────────────────────────────────────
# 웹 프레임워크
# ───────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3

# ───────────────────────────────────────────────
# 데이터베이스 (ORM)
# ───────────────────────────────────────────────
sqlalchemy==2.0.41
pymysql==1.1.1

# ───────────────────────────────────────────────
# 환경변수 관리
# ───────────────────────────────────────────────
python-dotenv==1.1.0

# ───────────────────────────────────────────────
# 인증 (비밀번호 + JWT 토큰)
# ───────────────────────────────────────────────
pwdlib[argon2]==0.3.0
PyJWT==2.13.0

# ───────────────────────────────────────────────
# 요청 데이터 검증
# ───────────────────────────────────────────────
email-validator==2.2.0

# ───────────────────────────────────────────────
# 데이터 수집 및 공휴일 처리
# ───────────────────────────────────────────────
requests==2.32.3
holidays==0.72

# ───────────────────────────────────────────────
# ML 모델 학습
# ───────────────────────────────────────────────
joblib==1.5.3               # pkl 저장/로드
scikit-learn==1.9.0          # RandomForestRegressor, LinearRegression
pandas                       # DataFrame 처리
numpy                        # 수치 연산 (matplotlib/seaborn 의존성)
matplotlib                   # EDA 시각화 (히스토그램, 산점도)
seaborn                      # EDA 시각화 (히트맵)

# ───────────────────────────────────────────────
# Jupyter 노트북 실행
# ───────────────────────────────────────────────
jupyter                      # train.ipynb 실행용 (PyCharm 내장 Jupyter 안 쓸 경우)

# ───────────────────────────────────────────────
# 미들웨어 및 파일 처리
# ───────────────────────────────────────────────
itsdangerous==2.2.0
python-multipart==0.0.20

# ───────────────────────────────────────────────
# 배포
# ───────────────────────────────────────────────
gunicorn                   # Railway 프로덕션 서버 (Procfile에서 사용)
# gunicorn --version

# ───────────────────────────────────────────────
# 실험 추적
# ───────────────────────────────────────────────
mlflow                     # MLflow 실험 추적 + 모델 아티팩트 관리


# ───────────────────────────────────────────────
# ML 모델 학습 추가 필
# ───────────────────────────────────────────────



# pip freeze > ~/hangang_parking/requirements.txt
# pip install -r requirements.txt